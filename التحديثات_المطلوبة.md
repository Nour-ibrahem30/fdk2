# 📋 التحديثات المطلوبة - دليل شامل

## ✅ ما تم إنجازه:

### 1. نظام امتحانات مدمج (exam-player.html)
✅ تم إنشاء صفحة لحل الامتحانات داخل المنصة
✅ دعم نوعين من الأسئلة: صح/خطأ + اختيار من متعدد
✅ مؤقت للامتحان
✅ عرض النتائج مع مراجعة الإجابات
✅ حفظ النتائج

## 🔧 ما يحتاج تحديث:

### 1. تحديث Dashboard لإضافة امتحانات مدمجة

في ملف `public/pages/dashboard.html`، ابحث عن زر "إضافة امتحان" وأضف خيار "نوع الامتحان":

```javascript
// في modal إضافة امتحان، أضف:
<div class="form-group">
    <label class="form-label">نوع الامتحان</label>
    <select class="form-select" id="examType" required>
        <option value="external">رابط خارجي (Google Forms)</option>
        <option value="internal">امتحان مدمج (داخل المنصة)</option>
    </select>
</div>

<!-- للامتحان الخارجي -->
<div class="form-group" id="externalExamGroup">
    <label class="form-label">رابط الامتحان</label>
    <input type="url" class="form-input" id="examUrl" placeholder="https://forms.google.com/...">
</div>

<!-- للامتحان المدمج -->
<div class="form-group" id="internalExamGroup" style="display: none;">
    <button type="button" class="btn btn-secondary" id="addQuestionBtn">➕ إضافة سؤال</button>
    <div id="questionsContainer"></div>
</div>

<script>
// إضافة listener لتغيير نوع الامتحان
document.getElementById('examType').addEventListener('change', function() {
    const isInternal = this.value === 'internal';
    document.getElementById('externalExamGroup').style.display = isInternal ? 'none' : 'block';
    document.getElementById('internalExamGroup').style.display = isInternal ? 'block' : 'none';
});

// إضافة سؤال جديد
let questionCount = 0;
document.getElementById('addQuestionBtn').addEventListener('click', function() {
    questionCount++;
    const questionHTML = `
        <div class="question-item" data-question-id="${questionCount}" style="background: rgba(15, 23, 42, 0.5); padding: 1.5rem; border-radius: 12px; margin-top: 1rem; border: 2px solid rgba(59, 130, 246, 0.2);">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                <h4 style="color: #3b82f6;">السؤال ${questionCount}</h4>
                <button type="button" class="btn btn-sm btn-danger" onclick="removeQuestion(${questionCount})">🗑️ حذف</button>
            </div>
            
            <div class="form-group">
                <label class="form-label">نص السؤال</label>
                <textarea class="form-textarea" id="question_${questionCount}_text" rows="2" required></textarea>
            </div>
            
            <div class="form-group">
                <label class="form-label">نوع السؤال</label>
                <select class="form-select" id="question_${questionCount}_type" onchange="updateQuestionOptions(${questionCount})">
                    <option value="true-false">صح أم خطأ</option>
                    <option value="mcq">اختيار من متعدد</option>
                </select>
            </div>
            
            <div id="question_${questionCount}_options">
                <!-- سيتم ملؤها بناءً على نوع السؤال -->
            </div>
            
            <div class="form-group">
                <label class="form-label">الإجابة الصحيحة</label>
                <select class="form-select" id="question_${questionCount}_correct" required>
                    <option value="0">الخيار الأول</option>
                    <option value="1">الخيار الثاني</option>
                </select>
            </div>
            
            <div class="form-group">
                <label class="form-label">الشرح (اختياري)</label>
                <textarea class="form-textarea" id="question_${questionCount}_explanation" rows="2"></textarea>
            </div>
        </div>
    `;
    
    document.getElementById('questionsContainer').insertAdjacentHTML('beforeend', questionHTML);
    updateQuestionOptions(questionCount);
});

// تحديث خيارات السؤال بناءً على نوعه
window.updateQuestionOptions = function(questionId) {
    const type = document.getElementById(`question_${questionId}_type`).value;
    const optionsContainer = document.getElementById(`question_${questionId}_options`);
    const correctSelect = document.getElementById(`question_${questionId}_correct`);
    
    if (type === 'true-false') {
        optionsContainer.innerHTML = `
            <div class="form-group">
                <label class="form-label">الخيارات</label>
                <input type="text" class="form-input" id="question_${questionId}_option_0" value="صح" readonly>
                <input type="text" class="form-input" id="question_${questionId}_option_1" value="خطأ" readonly style="margin-top: 0.5rem;">
            </div>
        `;
        correctSelect.innerHTML = `
            <option value="0">صح</option>
            <option value="1">خطأ</option>
        `;
    } else {
        optionsContainer.innerHTML = `
            <div class="form-group">
                <label class="form-label">الخيارات</label>
                <input type="text" class="form-input" id="question_${questionId}_option_0" placeholder="الخيار الأول" required>
                <input type="text" class="form-input" id="question_${questionId}_option_1" placeholder="الخيار الثاني" required style="margin-top: 0.5rem;">
                <input type="text" class="form-input" id="question_${questionId}_option_2" placeholder="الخيار الثالث" required style="margin-top: 0.5rem;">
                <input type="text" class="form-input" id="question_${questionId}_option_3" placeholder="الخيار الرابع" required style="margin-top: 0.5rem;">
            </div>
        `;
        correctSelect.innerHTML = `
            <option value="0">الخيار الأول</option>
            <option value="1">الخيار الثاني</option>
            <option value="2">الخيار الثالث</option>
            <option value="3">الخيار الرابع</option>
        `;
    }
};

// حذف سؤال
window.removeQuestion = function(questionId) {
    const questionEl = document.querySelector(`[data-question-id="${questionId}"]`);
    if (questionEl) questionEl.remove();
};

// عند حفظ الامتحان
const examType = document.getElementById('examType').value;

if (examType === 'internal') {
    // جمع الأسئلة
    const questions = [];
    document.querySelectorAll('.question-item').forEach((qEl) => {
        const qId = qEl.dataset.questionId;
        const type = document.getElementById(`question_${qId}_type`).value;
        const optionsCount = type === 'true-false' ? 2 : 4;
        
        const options = [];
        for (let i = 0; i < optionsCount; i++) {
            const optionEl = document.getElementById(`question_${qId}_option_${i}`);
            if (optionEl) options.push(optionEl.value);
        }
        
        questions.push({
            question: document.getElementById(`question_${qId}_text`).value,
            type: type,
            options: options,
            correctAnswer: parseInt(document.getElementById(`question_${qId}_correct`).value),
            explanation: document.getElementById(`question_${qId}_explanation`).value || ''
        });
    });
    
    const newExam = {
        title: title,
        duration: parseInt(duration),
        description: description,
        examType: 'internal',
        questions: questions,
        createdAt: new Date().toISOString()
    };
    
    // حفظ في Firebase
    return addDoc(collection(db, 'exams'), newExam)
        .then((docRef) => {
            newExam.id = docRef.id;
            exams.push(newExam);
            renderExams();
            updateStats();
            showToast('تم إضافة الامتحان بنجاح!', 'success');
            return true;
        })
        .catch((error) => {
            console.error('Failed to save exam:', error);
            showToast('خطأ في حفظ الامتحان: ' + error.message, 'error');
            return false;
        });
} else {
    // الكود الحالي للامتحان الخارجي
}
</script>
```

### 2. تحديث صفحة الامتحانات (exams.html)

في ملف `public/pages/exams.html`، عدّل دالة `displayExams`:

```javascript
function displayExams(exams) {
    if (exams.length === 0) {
        examsGrid.innerHTML = `
            <div class="empty-state" style="grid-column: 1/-1;">
                <div class="empty-state-icon">📋</div>
                <h3>لا توجد امتحانات متاحة</h3>
                <p>سيتم إضافة المحتوى قريباً</p>
            </div>
        `;
        return;
    }

    examsGrid.innerHTML = exams.map(exam => {
        // تحديد نوع الامتحان
        const isInternal = exam.examType === 'internal' && exam.questions && exam.questions.length > 0;
        const examLink = isInternal 
            ? `exam-player.html?id=${exam.id}` 
            : exam.examUrl;
        
        return `
            <div class="exam-card">
                <span class="exam-type">${isInternal ? 'امتحان مدمج' : 'امتحان خارجي'}</span>
                <h3 class="exam-title">${exam.title}</h3>
                ${exam.description ? `<p style="color: #94a3b8; margin-bottom: 1rem;">${exam.description}</p>` : ''}
                <div class="exam-info">
                    <span>
                        <span aria-hidden="true">⏱️</span>
                        ${exam.duration || 0} دقيقة
                    </span>
                    <span>
                        <span aria-hidden="true">📝</span>
                        ${isInternal ? exam.questions.length + ' سؤال' : 'اختبار تفاعلي'}
                    </span>
                </div>
                <div class="exam-actions">
                    <button class="btn-start" onclick="window.location.href='${examLink}'" aria-label="بدء ${exam.title}">
                        <span aria-hidden="true">🚀</span>
                        ابدأ الامتحان
                    </button>
                </div>
            </div>
        `;
    }).join('');
}
```

### 3. إضافة مشغل فيديو مدمج

أنشئ ملف `public/pages/video-player.html`:

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مشاهدة الفيديو | منصة الفيلسوف التعليمية</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Cairo', sans-serif;
            background: #000;
            color: #fff;
        }

        .video-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        .video-player {
            position: relative;
            width: 100%;
            padding-bottom: 56.25%; /* 16:9 aspect ratio */
            background: #000;
            border-radius: 12px;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .video-player iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .video-info {
            background: rgba(30, 41, 59, 0.8);
            padding: 2rem;
            border-radius: 12px;
            margin-bottom: 2rem;
        }

        .video-title {
            font-size: 2rem;
            margin-bottom: 1rem;
            color: #3b82f6;
        }

        .video-meta {
            display: flex;
            gap: 2rem;
            color: #94a3b8;
            margin-bottom: 1rem;
        }

        .video-description {
            color: #cbd5e1;
            line-height: 1.8;
        }

        .btn-back {
            display: inline-block;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            color: white;
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .btn-back:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
        }
    </style>
</head>
<body>
    <div class="video-container">
        <div class="video-player" id="videoPlayer">
            <div style="display: flex; align-items: center; justify-content: center; height: 100%; color: #94a3b8;">
                جاري تحميل الفيديو...
            </div>
        </div>

        <div class="video-info">
            <h1 class="video-title" id="videoTitle">عنوان الفيديو</h1>
            <div class="video-meta">
                <span id="videoDuration">⏱️ 0 دقيقة</span>
                <span id="videoSource">📹 YouTube</span>
            </div>
            <p class="video-description" id="videoDescription"></p>
        </div>

        <a href="videos.html" class="btn-back">← العودة للفيديوهات</a>
    </div>

    <script type="module">
        import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js';
        import { getFirestore, doc, getDoc } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js';

        const firebaseConfig = {
            apiKey: "AIzaSyAU0CCiQNrPEYpTNU4rAwmOmPUZnjb2FoU",
            authDomain: "a-platform-for-learning.firebaseapp.com",
            projectId: "a-platform-for-learning",
            storageBucket: "a-platform-for-learning.firebasestorage.app",
            messagingSenderId: "764579707883",
            appId: "1:764579707883:web:5456e2348354cc58fab7ae"
        };

        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);

        const urlParams = new URLSearchParams(window.location.search);
        const videoId = urlParams.get('id');

        async function loadVideo() {
            if (!videoId) {
                alert('لم يتم تحديد الفيديو');
                window.location.href = 'videos.html';
                return;
            }

            try {
                const videoDoc = await getDoc(doc(db, 'videos', videoId));
                
                if (!videoDoc.exists()) {
                    alert('الفيديو غير موجود');
                    window.location.href = 'videos.html';
                    return;
                }

                const video = videoDoc.data();
                
                // Display video info
                document.getElementById('videoTitle').textContent = video.title;
                document.getElementById('videoDuration').textContent = `⏱️ ${video.duration || 0} دقيقة`;
                document.getElementById('videoSource').textContent = `📹 ${video.source === 'youtube' ? 'YouTube' : 'مرفوع'}`;
                document.getElementById('videoDescription').textContent = video.notes || 'لا يوجد وصف';

                // Embed video
                const videoPlayer = document.getElementById('videoPlayer');
                
                if (video.source === 'youtube') {
                    const videoIdMatch = video.videoUrl.match(/(?:youtube\.com\/(?:[^\/]+\/.+\/|(?:v|e(?:mbed)?)\/|.*[?&]v=)|youtu\.be\/)([^"&?\/\s]{11})/);
                    
                    if (videoIdMatch && videoIdMatch[1]) {
                        videoPlayer.innerHTML = `
                            <iframe 
                                src="https://www.youtube.com/embed/${videoIdMatch[1]}" 
                                frameborder="0" 
                                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                                allowfullscreen
                            ></iframe>
                        `;
                    } else {
                        videoPlayer.innerHTML = '<p style="text-align: center; padding: 2rem;">رابط الفيديو غير صحيح</p>';
                    }
                } else {
                    videoPlayer.innerHTML = `
                        <video controls style="width: 100%; height: 100%;">
                            <source src="${video.videoUrl}" type="video/mp4">
                            متصفحك لا يدعم تشغيل الفيديو
                        </video>
                    `;
                }
            } catch (error) {
                console.error('Error loading video:', error);
                alert('خطأ في تحميل الفيديو');
                window.location.href = 'videos.html';
            }
        }

        loadVideo();
    </script>
</body>
</html>
```

### 4. تحديث صفحة الفيديوهات لاستخدام المشغل المدمج

في `public/pages/videos.html`، عدّل دالة `displayVideos`:

```javascript
videosGrid.innerHTML = videos.map(video => `
    <div class="video-card" role="listitem">
        <div class="video-thumbnail">
            <img src="${getYoutubeThumbnail(video.videoUrl)}" alt="${video.title}" loading="lazy">
            <span class="video-duration">${video.duration || 0} دقيقة</span>
        </div>
        <div class="video-content">
            <h3 class="video-title">${video.title}</h3>
            ${video.notes ? `<p class="video-description">${video.notes}</p>` : ''}
            <div class="video-meta">
                <span>📹 ${video.source === 'youtube' ? 'YouTube' : 'مرفوع'}</span>
                <span>⏱️ ${video.duration || 0} دقيقة</span>
            </div>
            <button class="btn btn-primary btn-watch" onclick="window.location.href='video-player.html?id=${video.id}'" aria-label="مشاهدة ${video.title}">
                <span aria-hidden="true">▶️</span>
                مشاهدة الفيديو
            </button>
        </div>
    </div>
`).join('');
```

## 📝 ملخص التحديثات:

### ✅ تم إنشاؤه:
1. **exam-player.html** - مشغل امتحانات مدمج
2. **video-player.html** - مشغل فيديو مدمج (في الكود أعلاه)

### 🔧 يحتاج تحديث:
1. **dashboard.html** - إضافة واجهة إنشاء امتحانات مدمجة
2. **exams.html** - دعم نوعي الامتحانات (مدمج وخارجي)
3. **videos.html** - استخدام المشغل المدمج
4. **notes.html** - إضافة Firebase (مثل videos.html)
5. **materials.html** - إضافة Firebase (مثل videos.html)

## 🎯 الأولويات:

1. **عالية جداً**: تحديث Dashboard لإضافة امتحانات مدمجة
2. **عالية**: تحديث exams.html لدعم النوعين
3. **متوسطة**: إضافة video-player.html وتحديث videos.html
4. **منخفضة**: تحديث notes.html و materials.html

هل تريد مني البدء بتنفيذ أي من هذه التحديثات؟
