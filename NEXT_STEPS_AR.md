# الخطوات التالية - ربط صفحات الفيديوهات والامتحانات بـ Firebase

## ✅ ما تم إنجازه:

1. ✅ تحويل لوحة التحكم (Dashboard) من localStorage إلى Firebase
2. ✅ المدرس يمكنه الآن إضافة/تعديل/حذف المحتوى من Firebase
3. ✅ تحديث قواعد الأمان (Firestore Rules)

## 📋 ما يحتاج إلى العمل عليه:

### 1. صفحة الفيديوهات (Videos Page)
**الملف**: `public/pages/videos.html` أو ملف TypeScript مشابه

**ما يجب تغييره**:
- تحميل الفيديوهات من Firebase collection `videos` بدلاً من localStorage
- التحقق من أن المستخدم مسجل دخول قبل عرض الفيديوهات
- عرض الفيديوهات التي أضافها المدرس من لوحة التحكم

**مثال على الكود**:
```javascript
import { getFirestore, collection, getDocs } from 'firebase/firestore';

const db = getFirestore();

async function loadVideos() {
  const videosSnapshot = await getDocs(collection(db, 'videos'));
  const videos = videosSnapshot.docs.map(doc => ({
    id: doc.id,
    ...doc.data()
  }));
  
  // عرض الفيديوهات في الصفحة
  displayVideos(videos);
}
```

### 2. صفحة الامتحانات (Exams Page)
**الملف**: `public/pages/exams.html` أو ملف TypeScript مشابه

**ما يجب تغييره**:
- تحميل الامتحانات من Firebase collection `exams` بدلاً من localStorage
- التحقق من أن المستخدم مسجل دخول قبل عرض الامتحانات
- عرض الامتحانات التي أضافها المدرس من لوحة التحكم

**مثال على الكود**:
```javascript
import { getFirestore, collection, getDocs } from 'firebase/firestore';

const db = getFirestore();

async function loadExams() {
  const examsSnapshot = await getDocs(collection(db, 'exams'));
  const exams = examsSnapshot.docs.map(doc => ({
    id: doc.id,
    ...doc.data()
  }));
  
  // عرض الامتحانات في الصفحة
  displayExams(exams);
}
```

### 3. صفحة الملاحظات (Notes Page)
**الملف**: `public/pages/notes.html` أو ملف TypeScript مشابه

**ما يجب تغييره**:
- تحميل الملاحظات من Firebase collection `notes`
- عرض الملاحظات التي أضافها المدرس

### 4. صفحة المذكرات (Materials Page)
**الملف**: `public/pages/materials.html` أو ملف TypeScript مشابه

**ما يجب تغييره**:
- تحميل المذكرات من Firebase collection `materials`
- تصنيف المذكرات حسب الصف الدراسي

### 5. الصفحة الرئيسية (Home Page)
**الملف**: `public/pages/home.html` أو `index.html`

**ما يجب تغييره**:
- عرض آراء الطلاب من Firebase collection `testimonials`
- عرض إحصائيات من Firebase (عدد الفيديوهات، الامتحانات، إلخ)

## 🔍 كيفية العثور على الملفات:

استخدم البحث في المشروع للعثور على:
- `videos.html` أو `videos.ts`
- `exams.html` أو `exams.ts`
- `notes.html` أو `notes.ts`
- `materials.html` أو `materials.ts`

## 📝 نموذج التحويل:

### قبل (localStorage):
```javascript
const videos = JSON.parse(localStorage.getItem('videos') || '[]');
```

### بعد (Firebase):
```javascript
const videosSnapshot = await getDocs(collection(db, 'videos'));
const videos = videosSnapshot.docs.map(doc => ({ id: doc.id, ...doc.data() }));
```

## ⚠️ نقاط مهمة:

1. **التحقق من تسجيل الدخول**: تأكد من أن المستخدم مسجل دخول قبل عرض المحتوى
2. **معالجة الأخطاء**: استخدم try-catch لمعالجة أخطاء Firebase
3. **Loading State**: أضف مؤشر تحميل أثناء جلب البيانات من Firebase
4. **Real-time Updates**: يمكنك استخدام `onSnapshot` بدلاً من `getDocs` للحصول على تحديثات فورية

## 🎯 الأولوية:

1. **أولوية عالية**: صفحة الفيديوهات (Videos)
2. **أولوية عالية**: صفحة الامتحانات (Exams)
3. **أولوية متوسطة**: صفحة الملاحظات (Notes)
4. **أولوية متوسطة**: صفحة المذكرات (Materials)
5. **أولوية منخفضة**: الصفحة الرئيسية (Testimonials)

## 💡 نصيحة:

ابدأ بصفحة واحدة (مثل الفيديوهات) واختبرها جيداً قبل الانتقال إلى الصفحات الأخرى. هذا سيساعدك على فهم النمط وتطبيقه على باقي الصفحات.

---

**هل تريد المساعدة في تحويل صفحة معينة؟** أخبرني وسأساعدك!
