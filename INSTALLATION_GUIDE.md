# دليل التثبيت الشامل - منصة الفيلسوف التعليمية

## نظرة عامة

منصة تعليمية متكاملة للأستاذ محمد ناصر "الفيلسوف" تتضمن:
- 🎥 إدارة الفيديوهات التعليمية
- 📋 نظام الامتحانات التفاعلية
- 📝 نظام الملاحظات والإعلانات
- 🔔 الإشعارات الفورية
- 📊 تتبع تقدم الطلاب
- 🎯 نظام الوصول المشروط للمحتوى

---

## المتطلبات الأساسية

### 1. البرامج المطلوبة:
- **Node.js** (الإصدار 16 أو أحدث)
- **npm** (يأتي مع Node.js)
- **Git** (اختياري)

### 2. قاعدة البيانات:
- **MongoDB Atlas** (سحابية) - مُعدة مسبقاً
- أو **MongoDB** محلية

---

## خطوات التثبيت

### الخطوة 1: تحميل المشروع
```bash
# إذا كان لديك Git
git clone <repository-url>
cd philosopher-learning-platform

# أو قم بتحميل الملفات وفك الضغط
```

### الخطوة 2: تثبيت التبعيات
```bash
# تثبيت تبعيات الخادم والواجهة الأمامية معاً
npm run install-all

# أو تثبيت كل منهما منفصلاً:
npm install
cd client && npm install && cd ..
```

### الخطوة 3: إعداد قاعدة البيانات
1. افتح ملف `.env`
2. استبدل `<db_password>` بكلمة المرور الفعلية:
```env
MONGODB_URI=mongodb+srv://nouribrahem207_db_user:YOUR_PASSWORD@cluster0.8q2ioti.mongodb.net/philosopher_platform?retryWrites=true&w=majority
```

### الخطوة 4: إعداد البيانات التجريبية
```bash
npm run setup
```

### الخطوة 5: تشغيل المشروع
```bash
# الطريقة الأولى: تشغيل كل شيء معاً (Windows)
start.bat

# الطريقة الثانية: تشغيل منفصل
# Terminal 1 - الخادم
npm run server

# Terminal 2 - الواجهة الأمامية
npm run client
```

---

## الوصول للمنصة

### الروابط:
- **الواجهة الأمامية:** http://localhost:3000
- **API الخادم:** http://localhost:5000

### الحسابات التجريبية:

#### المدرس (الأستاذ محمد ناصر "الفيلسوف"):
- **البريد:** teacher@philosopher.com
- **كلمة المرور:** 123456

#### الطلاب:
- **البريد:** student1@philosopher.com (أو student2, student3, student4, student5)
- **كلمة المرور:** 123456

---

## الميزات المتاحة

### للمدرس:
✅ **لوحة تحكم شاملة** - إحصائيات مفصلة عن الطلاب  
✅ **إدارة الفيديوهات** - رفع وتنظيم المحتوى التعليمي  
✅ **إنشاء الامتحانات** - أسئلة متنوعة مع تصحيح تلقائي  
✅ **نظام الملاحظات** - إرسال رسائل وتنبيهات للطلاب  
✅ **متابعة الطلاب** - تتبع تقدم كل طالب على حدة  
✅ **الإشعارات الفورية** - تنبيهات لحظية للطلاب  

### للطلاب:
✅ **الوصول المشروط** - مشاهدة الفيديوهات بعد اجتياز الامتحانات  
✅ **ملف شخصي تفاعلي** - تتبع التقدم والإحصائيات  
✅ **إشعارات فورية** - تنبيهات عند إضافة محتوى جديد  
✅ **نظام النقاط** - متابعة الأداء والتحسن  
✅ **مكتبة شاملة** - الوصول لجميع المواد التعليمية  

---

## البيانات التجريبية المتضمنة

### المستخدمين:
- 1 مدرس (الأستاذ محمد ناصر)
- 5 طلاب بدرجات وإحصائيات متنوعة

### المحتوى:
- 2 امتحان تجريبي (الفلسفة والمنطق)
- 2 فيديو تعليمي مرتبط بالامتحانات
- 2 ملاحظة (ترحيب وتذكير)
- إحصائيات وبيانات أداء واقعية

---

## استكشاف الأخطاء

### مشاكل قاعدة البيانات:
```bash
# خطأ المصادقة
MongoServerError: bad auth: Authentication failed
```
**الحل:** تأكد من صحة كلمة المرور في ملف `.env`

```bash
# خطأ الشبكة
MongoNetworkError: connection timed out
```
**الحل:** تحقق من إعدادات Network Access في MongoDB Atlas

### مشاكل المنافذ:
```bash
# المنفذ مستخدم
Error: listen EADDRINUSE :::5000
```
**الحل:** أغلق التطبيقات التي تستخدم المنافذ 5000 أو 3000

### مشاكل التبعيات:
```bash
# تبعيات مفقودة
Module not found
```
**الحل:** 
```bash
rm -rf node_modules client/node_modules
npm run install-all
```

---

## الإعدادات المتقدمة

### تخصيص المنافذ:
```env
# في ملف .env
PORT=5000
CLIENT_PORT=3000
```

### إعدادات البريد الإلكتروني:
```env
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
```

### إعدادات الأمان:
```env
JWT_SECRET=your_very_secure_secret_key
```

---

## النشر في الإنتاج

### 1. بناء الواجهة الأمامية:
```bash
cd client
npm run build
cd ..
```

### 2. تشغيل الخادم:
```bash
NODE_ENV=production npm start
```

### 3. إعدادات الإنتاج:
```env
NODE_ENV=production
MONGODB_URI=your_production_mongodb_uri
JWT_SECRET=your_production_secret
```

---

## الدعم والمساعدة

### الملفات المرجعية:
- `README.md` - معلومات عامة عن المشروع
- `QUICK_START.md` - دليل التشغيل السريع
- `DATABASE_SETUP.md` - إعداد قاعدة البيانات

### للحصول على المساعدة:
1. راجع ملفات التوثيق
2. تحقق من سجلات الأخطاء في Terminal
3. تأكد من تشغيل جميع الخدمات المطلوبة

---

## الخطوات التالية

بعد التثبيت الناجح:

1. **جرب تسجيل الدخول** بحساب المدرس واستكشف لوحة التحكم
2. **أنشئ امتحان جديد** وشاهد كيف يعمل النظام
3. **سجل دخول كطالب** وجرب حل الامتحانات
4. **اختبر الإشعارات** بإضافة ملاحظة جديدة
5. **استكشف الإحصائيات** ومتابعة تقدم الطلاب

---

🎉 **تهانينا! منصة الفيلسوف التعليمية جاهزة للاستخدام**

*تم تطوير هذه المنصة خصيصاً للأستاذ محمد ناصر "الفيلسوف" لتوفير تجربة تعليمية متميزة وتفاعلية.*